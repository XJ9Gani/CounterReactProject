import { createContext } from "react";

const ContextUseState = createContext(undefined);
const ContextReducer = createContext(undefined);
const ContextCustomHook = createContext(undefined);

export { ContextUseState, ContextReducer, ContextCustomHook };
